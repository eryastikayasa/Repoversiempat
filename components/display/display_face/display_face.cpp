#include "display_face.h"

void display_face_init(void) {
    // Structure only. Existing face implementation is untouched.
}

void display_face_update(uint32_t now_ms) {
    (void)now_ms;
    // Structure only. Face animation will be migrated here later.
}
