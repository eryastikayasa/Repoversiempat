#include "display_text.h"

void display_text_init(void) {
    // Structure only. Existing text implementation is untouched.
}

void display_text_update(uint32_t now_ms) {
    (void)now_ms;
    // Structure only. Text behavior will be migrated here later.
}
